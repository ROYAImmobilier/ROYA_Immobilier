import 'package:flutter/material.dart';

class ContactSend extends StatelessWidget {
  const ContactSend({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [

        ],
      ),
    );
  }
}
